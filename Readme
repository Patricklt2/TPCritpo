
# TPCripto
## Se agarra el current working directory de donde uno ejecuta el programa
## Cualquier carpeta que se quiera agregar por simplicidad de lectura de archivos, seteamos como base src/, si se agrega una carpeta assets/, encodings/, etc debe ser ahí.
## Si 

# Ejemplo de uso:

## Compila el proyecto y genera el ejecutable en ./bin/main.out
make all
## Ejecutar el programa con los parámetros deseados:
/bin/main.out -d --secret Albertssd.bmp -k 8 -n 8 --dir assets/

./bin/main.out -r --secret hola.bmp -k 8 -n 8 --dir encodings/

## Tener en cuenta que el path de base de los archivos que se pasan es ./src/ 